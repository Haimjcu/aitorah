import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'App Directory — AI Torah' }

export default function AppsLayout({ children }: { children: React.ReactNode }) {
  return children
}
